# QA Flow Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add a full QA/testing workstream — 6 new skills (`qa-flow`, `qa-brief-intake`, `qa-strategy`, `qa-cases`, `qa-tasks`, `qa-review`) plus a new `testingRigor` project profile field (`mvp | full`) — and wire 8 existing skills to consume it.

**Architecture:** Mirrors the backend/frontend pattern (brief-intake → stack-equivalent → content → tasks → review). `testingRigor` lives in `PROJECT_PROFILE.md` and branches `qa-strategy` + `qa-review` output shape. New artifacts land in `.qa/<feature>/`. Filename contracts for existing artifacts preserved.

**Tech stack:** Markdown skill definitions. No runtime code. Verification is per-skill spec + code-quality review + end-to-end read-through.

**Design doc:** `docs/plans/2026-04-19-qa-flow-design.md` — read before starting.

---

## Conventions for every task

Each task:
1. Read relevant files (existing skills for context, plus this plan).
2. Apply edits (Edit for surgical changes; Write for new skill files).
3. Verify with `grep` (each task specifies the exact queries).
4. Commit. Commit format: `<skill-name>: <action>` (imperative, ≤70 chars).

No "write failing test" step — these are skill specs. Verification is that the new spec is readable, placement is right, and cross-references between skills match.

**Reference skill for new qa-* files:** Use `backend-brief-intake`, `backend-stack`, `backend-data`, `backend-api`, `backend-tasks`, `backend-review` as your style guide. The qa-* skills should feel like siblings — same tone, structure (## How to run → Steps → ## Rules), and artifact format.

**PROJECT_PROFILE.md schema reminder:** After all these tasks, the file format is:
```markdown
# Project Profile
designMode: shadcn-theme | custom-system
uiFramework: shadcn/ui | <other> | none
testingRigor: mvp | full
```

---

## Task 1: pm-grill — add Testing profile branch + extend Project Profile section

**File:** `skills/pm-grill/SKILL.md`

**Step 1:** Read the file.

**Step 2:** In Mode B § Step B2 "Technical branches", find the bullet:
```
- Delivery profile: Design mode — `shadcn-theme` or `custom-system`? UI framework — `shadcn/ui`, other, or none?
```

Insert a new bullet immediately AFTER it:
```
- Testing profile: Testing rigor — `mvp` (smoke pyramid, 1 E2E per journey, covers only FR must) or `full` (full pyramid, all FR covered, visual regression + a11y + perf)?
```

**Step 3:** Extend the "Project Profile (both modes)" section. Find the format block:
```markdown
# Project Profile
designMode: shadcn-theme | custom-system
uiFramework: shadcn/ui | <other> | none
```

Replace with:
```markdown
# Project Profile
designMode: shadcn-theme | custom-system
uiFramework: shadcn/ui | <other> | none
testingRigor: mvp | full
```

Then find the prose sentence in that section listing downstream skills:
```
When the Delivery profile branch is resolved, ALSO write `.pm/<feature-name>/PROJECT_PROFILE.md` — a minimal, machine-readable record that downstream skills (`pm-handoff`, `design-flow`, `design-tokens`, `design-components`, `frontend-stack`, `frontend-components`) read with a simple grep.
```

Replace with:
```
When the Delivery profile OR Testing profile branch is resolved, ALSO write `.pm/<feature-name>/PROJECT_PROFILE.md` — a minimal, machine-readable record that downstream skills (`pm-handoff`, `pm-architecture`, `design-flow`, `design-tokens`, `design-components`, `frontend-stack`, `frontend-components`, `qa-strategy`, `qa-review`) read with a simple grep.
```

**Step 4:** Verify:
- `grep -n "Testing profile" skills/pm-grill/SKILL.md` → 1 match in Mode B § Step B2.
- `grep -n "testingRigor" skills/pm-grill/SKILL.md` → at least 1 match in the Project Profile format block.
- `grep -n "qa-strategy" skills/pm-grill/SKILL.md` → 1 match in the downstream skills sentence.

**Step 5:** Commit: `pm-grill: add Testing profile branch and testingRigor schema`

---

## Task 2: pm-intake — detect test infra / testingRigor

**File:** `skills/pm-intake/SKILL.md`

**Step 1:** Read the file.

**Step 2:** In Step 2 "Technical categories" (the 7-bullet list), find the last bullet:
```
- UI framework or component library mentioned (shadcn/ui, MUI, Chakra, custom design system)
```

Insert a new bullet immediately AFTER it:
```
- Testing infrastructure / rigor — test framework mentioned (Vitest, Jest, Playwright, Cypress, pytest), coverage targets, CI gates (used to decide `testingRigor` in PROJECT_PROFILE.md)
```

**Step 3:** In Step 3 classification examples table, append two rows after the existing 6:
```
| "Playwright E2E with 80% coverage" | ✅ Clear | Testing stack + target set → `testingRigor = full` inferível |
| "(no testing strategy mentioned)" | ⚠️ Ambiguous | Gap importante — resolvable in qa-strategy if not resolved in grill |
```

**Step 4:** Verify:
- `grep -n "testingRigor" skills/pm-intake/SKILL.md` → at least 2 matches.
- `grep -n "Playwright E2E" skills/pm-intake/SKILL.md` → 1 match.

**Step 5:** Commit: `pm-intake: detect test infra as classified element`

---

## Task 3: Create `skills/qa-flow/SKILL.md`

**New file.**

**Step 1:** Read `skills/backend-flow/SKILL.md` and `skills/frontend-flow/SKILL.md` for structure/voice reference.

**Step 2:** Create `skills/qa-flow/SKILL.md` with this content (use Write tool):

```markdown
---
name: qa-flow
description: Orchestrates the full QA definition pipeline for a named project or feature — intake → strategy → cases → tasks → review. Reads `.qa/<feature>/QA_BRIEF.md` (from pm-handoff) or asks the user to paste one, then chains the specialist qa-* skills with confirmations between steps. Use when user says "qa flow", "start the qa", "plan the testing", or when pm-handoff invokes this skill. Not for ad-hoc test debugging, generic QA consulting, or single-step work — each sub-step has its own skill (qa-brief-intake, qa-strategy, qa-cases, qa-tasks, qa-review).
---

# QA Flow — Orchestrator

Coordinates the in-package qa-* skills as a guided sequence.

## Sequence

1. `qa-brief-intake` — ingest QA_BRIEF.md + audit existing test infra + gap-fill grill
2. `qa-strategy` — test pyramid, tools by level, coverage targets, risk matrix, test data strategy
3. `qa-cases` — PRD FR-XXX + user journeys → Gherkin / Given-When-Then test cases
4. `qa-tasks` — vertical-sliced QA task list
5. (external implementation and build)
6. `qa-review` — post-build test execution report + failure triage + regression gaps

## How to run

### Step 1: Setup

Check that `.qa/<feature>/` exists. If invoked by pm-handoff, the slug is already passed. Otherwise ask: "Which feature/project?" Use a kebab-case slug.

If artifacts already exist, show what's present and ask: "Continue from where we left off, or start fresh?"

### Step 1.5: Read the project profile

Read `.pm/<feature>/PROJECT_PROFILE.md` to get `testingRigor` and `designMode`. If the file is missing:

> "Este projeto não tem PROJECT_PROFILE.md. Modo de design: **shadcn-theme** ou **custom-system**? Rigor de teste: **mvp** ou **full**? (Respostas gravam em `.pm/<feature-name>/PROJECT_PROFILE.md` — afetam design-tokens, design-components, qa-strategy, qa-review.)"

Write the file with the answers. Then announce the mode:

> "Rodando qa-flow em modo **<testingRigor>**. `qa-strategy` e `qa-review` vão se ajustar a esse rigor."

### Step 2: Locate the brief

Look for `.qa/<feature>/QA_BRIEF.md`. If absent, fall back to `.pm/<feature>/PRD.md` + `.pm/<feature>/ARCHITECTURE.md`. If neither exists, tell the user: "I need either a QA_BRIEF.md from pm-handoff or at least a PRD + architecture pair."

### Step 3: Run each sub-skill in sequence

For each step:

1. Announce: "**Step N/5: [skill name]** — [one-line description]"
2. Invoke the skill
3. Save output to `.qa/<feature>/`
4. Summarize what was produced
5. Ask: "Ready to continue? You can also skip, go back, stop, or redo."

### Step 4: Navigation

- `next` / `continue` → proceed
- `skip` → flag artifact as missing and continue
- `back` → re-run prior step
- `redo` → re-run current step
- `stop` → end, summarize what's produced

### Step 5: Pre-review gate

After `qa-tasks` completes, pause:

> "QA plan complete. Hand this to the QA team (or the devs, if solo) to implement per QA_TASKS.md. Come back to `qa-review` once the build is running and tests are executing."

### Step 6: Post-build review

When the user returns with "qa review" or similar, invoke `qa-review`.

### Step 7: Final summary

```
## QA Flow Complete: [Feature]

### Artifacts (.qa/<feature>/):
- ✅ QA_INTAKE.md
- ✅ QA_STRATEGY.md
- ✅ TEST_CASES.md
- ✅ QA_TASKS.md
- ✅ QA_REVIEW.md (post-build)

### What to do next:
- Implement automated tests per QA_TASKS.md
- Coordinate E2E test runs with backend and frontend workstreams
- Return for `qa-review` once the build is up and tests have executed
```

## Rules

- Save outputs incrementally — stopping at step 3 still leaves steps 1–3.
- Every sub-skill reads inputs from disk, not conversation history — safe to resume across sessions.
- Always read `.pm/<feature>/PROJECT_PROFILE.md` at Step 1.5. `testingRigor` changes what `qa-strategy` and `qa-review` produce.
- This skill does not execute tests. It plans them. Execution happens outside the flow.
- If the brief is thin, `qa-brief-intake` grills the gaps — do not bypass that by jumping to `qa-strategy`.
```

**Step 3:** Verify:
- File exists: `ls skills/qa-flow/SKILL.md`.
- `grep -n "testingRigor" skills/qa-flow/SKILL.md` → at least 2 matches.

**Step 4:** Commit: `qa-flow: add orchestrator skill`

---

## Task 4: Create `skills/qa-brief-intake/SKILL.md`

**New file.**

**Step 1:** Read `skills/backend-brief-intake/SKILL.md` as the structural reference.

**Step 2:** Create `skills/qa-brief-intake/SKILL.md`. The skill must:
- Read `.qa/<feature>/QA_BRIEF.md` (or fall back to PRD.md + ARCHITECTURE.md)
- Audit the codebase for existing test infrastructure: test files, test frameworks in package.json / pyproject.toml / go.mod, CI config (.github/workflows, .gitlab-ci.yml, .circleci), coverage reports, fixtures directories
- Classify every QA concern as ✅ clear / ⚠️ ambiguous / ❌ missing. Required concerns: test framework (unit), test framework (integration), test framework (E2E), test runner / CI, coverage tool, coverage target, test data / fixtures strategy, mocking strategy, a11y testing tooling, perf testing tooling, visual regression tooling, bug tracking integration
- Run a gap-fill grill on ambiguous + missing items
- Produce `.qa/<feature>/QA_INTAKE.md` with: Testing context summary, Codebase audit (what exists vs what's missing), Grill resolutions, Open questions, Ready-for-qa-strategy assessment

Use the backend-brief-intake template as the shape — same section headers, same style of classification table. Adapt specifics to testing domain.

Include frontmatter:
```
---
name: qa-brief-intake
description: Reads `.qa/<feature>/QA_BRIEF.md` (from pm-handoff or fallback to PRD + ARCHITECTURE), audits the existing test infrastructure in the codebase, classifies each QA concern as clear/ambiguous/missing, runs an embedded gap-fill grill on the ambiguous and missing items, and produces `.qa/<feature>/QA_INTAKE.md`. Use when qa-flow invokes it or when user says "intake the qa brief", "audit the test setup". Not for generic QA consulting or for replanning PM-level scope.
---
```

**Step 3:** Verify:
- File exists.
- `grep -n "QA_INTAKE" skills/qa-brief-intake/SKILL.md` → at least 2 matches.
- `grep -n "Codebase audit" skills/qa-brief-intake/SKILL.md` → at least 1 match.

**Step 4:** Commit: `qa-brief-intake: add codebase test-infra audit skill`

---

## Task 5: Create `skills/qa-strategy/SKILL.md`

**New file.**

**Step 1:** Read `skills/backend-stack/SKILL.md` as the structural reference (it's the decision-document skill — qa-strategy mirrors that shape).

**Step 2:** Create `skills/qa-strategy/SKILL.md`. The skill must:

- Read `.pm/<feature>/PROJECT_PROFILE.md` to get `testingRigor`
- Read `.qa/<feature>/QA_INTAKE.md`, `.pm/<feature>/PRD.md`, `.pm/<feature>/ARCHITECTURE.md`
- Branch on `testingRigor`:
  - `mvp` mode produces a slim strategy: smoke pyramid (unit where risk-weighted, 1 E2E per critical journey, integration tests deferred), Playwright for E2E + Vitest/pytest for unit (or whatever the project already uses), coverage target "reasonable" not numeric, no formal a11y / perf / visual regression matrix
  - `full` mode produces the full strategy: complete pyramid (unit per module, integration per boundary, E2E per journey + edge cases), tool decisions per layer, explicit numeric coverage targets (e.g., 80% lines, 70% branches), a11y matrix (WCAG AA per component), perf budgets (LCP/INP/CLS per route), visual regression (Percy, Chromatic, or Playwright snapshots)
- Produce `.qa/<feature>/QA_STRATEGY.md` with:
  - `testingRigor` declared in header
  - Test pyramid diagram (text art)
  - Tool decisions table (layer / tool / rationale)
  - Coverage targets
  - Risk matrix (a11y, perf, security — specifics depend on rigor)
  - Test data strategy (fixtures, seeds, production-like data handling)
  - CI integration (which gates block merge)
  - Boundaries with dev tasks (who tests unit / integration, who tests E2E)

Frontmatter:
```
---
name: qa-strategy
description: Reads `.qa/<feature>/QA_INTAKE.md`, `.pm/<feature>/PRD.md`, and `.pm/<feature>/PROJECT_PROFILE.md` (for `testingRigor`), and produces `.qa/<feature>/QA_STRATEGY.md` — the canonical test strategy document (pyramid, tools per layer, coverage targets, risk matrix for a11y/perf/sec, test data strategy, CI gates). Branches behavior on `testingRigor`: `mvp` mode outputs a smoke pyramid; `full` mode outputs a complete pyramid with explicit coverage targets and a11y/perf/visual-regression matrices. Use when qa-flow invokes it or when user says "define the test strategy". Not for executing tests or for ad-hoc test debugging.
---
```

Structure follows backend-stack's shape: `## How to run` with numbered Steps, including `### Step 1: Load inputs`, `### Step 1.5: Branch on testingRigor`, content steps, and ending with `## Rules`.

**Step 3:** Verify:
- File exists.
- `grep -n "testingRigor" skills/qa-strategy/SKILL.md` → at least 3 matches.
- `grep -n "mvp" skills/qa-strategy/SKILL.md` → at least 2 matches.
- `grep -n "full" skills/qa-strategy/SKILL.md` → at least 2 matches.

**Step 4:** Commit: `qa-strategy: add test strategy skill with testingRigor branching`

---

## Task 6: Create `skills/qa-cases/SKILL.md`

**New file.**

**Step 1:** Read `skills/backend-data/SKILL.md` as structural reference (content-layer skill).

**Step 2:** Create `skills/qa-cases/SKILL.md`. The skill must:

- Read `.pm/<feature>/PRD.md` (FR-XXX and user journeys), `.qa/<feature>/QA_STRATEGY.md` (to respect pyramid decisions), `.design/<feature>/COMPONENT_SPECS.md` if present (a11y requirements per component)
- Branch on `testingRigor`: `mvp` covers only FR-XXX marked "must"; `full` covers every FR-XXX plus edge cases and negative paths
- Convert each FR-XXX and user journey into structured test cases using Gherkin (Given / When / Then) — with explicit `Scenario:` headers and `Examples:` tables where data variation matters
- Group cases by: user journey (E2E), component (integration / unit), API endpoint (backend integration)
- Produce `.qa/<feature>/TEST_CASES.md` with:
  - Header: feature name, `testingRigor`, source artifacts
  - Coverage matrix: FR-XXX → test case IDs
  - Journey-level E2E cases (Gherkin)
  - Component-level cases (Gherkin, for interactive components)
  - API-level cases (Gherkin, one `Scenario:` per endpoint × state)
  - Negative cases section (for `full` rigor only)
  - Flakiness notes / known fragile selectors

Frontmatter:
```
---
name: qa-cases
description: Reads `.pm/<feature>/PRD.md` (FR-XXX + journeys), `.qa/<feature>/QA_STRATEGY.md`, and design component specs, then produces `.qa/<feature>/TEST_CASES.md` — structured Gherkin test cases (Given/When/Then) grouped by journey, component, and API endpoint. Coverage scope depends on `testingRigor` from PROJECT_PROFILE.md (`mvp` covers must-have FR only; `full` covers every FR plus edge and negative cases). Use when qa-flow invokes it or when user says "write the test cases", "gherkin the requirements". Not for implementing tests (that's qa-tasks) or for exploratory testing.
---
```

**Step 3:** Verify:
- File exists.
- `grep -n "Given" skills/qa-cases/SKILL.md` → at least 2 matches (Gherkin convention).
- `grep -n "testingRigor" skills/qa-cases/SKILL.md` → at least 2 matches.
- `grep -n "FR-XXX" skills/qa-cases/SKILL.md` → at least 2 matches.

**Step 4:** Commit: `qa-cases: add Gherkin test case generation skill`

---

## Task 7: Create `skills/qa-tasks/SKILL.md`

**New file.**

**Step 1:** Read `skills/backend-tasks/SKILL.md` as the structural reference — this is the vertical-slice skill pattern.

**Step 2:** Create `skills/qa-tasks/SKILL.md`. The skill must:

- Read `.qa/<feature>/QA_STRATEGY.md`, `.qa/<feature>/TEST_CASES.md`, `.qa/<feature>/QA_INTAKE.md`
- Decompose the test cases into vertical slices. Each slice = case-group + automation code + fixtures/seed data + CI wiring, sized for 1–2 days of work
- Order slices by risk (critical-path first) and dependency (fixtures before dependent cases)
- Produce `.qa/<feature>/QA_TASKS.md` with:
  - Header (feature, testingRigor, count of slices)
  - Task list, each task with: ID, title, description, Effort (Small/Medium/Large to match kanban-sync Effort field), dependencies (Blocks / Blocked by), Done when (acceptance checklist), test case IDs covered, files created, CI integration points

Match the task shape emitted by backend-tasks and frontend-tasks so `kanban-sync` can ingest QA_TASKS.md with no schema translation.

Frontmatter:
```
---
name: qa-tasks
description: Reads `.qa/<feature>/QA_STRATEGY.md` and `TEST_CASES.md` and produces `.qa/<feature>/QA_TASKS.md` — vertical-sliced QA implementation tasks (each slice = case group + automation + fixtures + CI wiring). Task shape matches backend-tasks and frontend-tasks so kanban-sync ingests it with no translation. Use when qa-flow invokes it or when user says "decompose the qa work", "plan the test automation". Not for writing actual test code (that's execution, outside this pack) or for defining the strategy (that's qa-strategy).
---
```

**Step 3:** Verify:
- File exists.
- `grep -n "Effort" skills/qa-tasks/SKILL.md` → at least 1 match.
- `grep -n "vertical slice" skills/qa-tasks/SKILL.md` → at least 1 match (case insensitive ok).

**Step 4:** Commit: `qa-tasks: add vertical-slice QA task decomposition skill`

---

## Task 8: Create `skills/qa-review/SKILL.md`

**New file.**

**Step 1:** Read `skills/backend-review/SKILL.md` and `skills/frontend-review/SKILL.md` as references.

**Step 2:** Create `skills/qa-review/SKILL.md`. The skill must:

- Run post-build — expects an actual built app and test runs available
- Read `.pm/<feature>/PROJECT_PROFILE.md` to get `testingRigor`
- Read `.qa/<feature>/QA_STRATEGY.md`, `TEST_CASES.md`, `QA_TASKS.md`
- Branch on `testingRigor`:
  - `mvp`: smoke E2E pass/fail matrix + manual spot-check report + critical-path regression checklist. Skips visual regression, formal a11y matrix, perf budget.
  - `full`: full matrix — every test case's pass/fail status, coverage report vs targets, visual regression diffs summary, a11y matrix (WCAG AA per component), perf budget compliance (LCP/INP/CLS per route), regression gap analysis.
- Produce `.qa/<feature>/QA_REVIEW.md` with:
  - Header (feature, testingRigor, review date, build SHA)
  - Summary: green/yellow/red status
  - Pass/fail matrix by test case
  - Coverage report (if `full`)
  - Failures section: triage per failure (test bug / product bug / infra issue) + reproduction steps + suggested fix owner
  - Regression gaps (what should have been tested but isn't covered)
  - Follow-up tasks (link to items to add to QA_TASKS.md)

Frontmatter:
```
---
name: qa-review
description: Runs after the build is live. Reads `.qa/<feature>/QA_STRATEGY.md`, `TEST_CASES.md`, test execution output, and `.pm/<feature>/PROJECT_PROFILE.md` (for `testingRigor`). Produces `.qa/<feature>/QA_REVIEW.md` — pass/fail matrix per test case, triage of failures (test bug vs product bug vs infra issue), regression gaps, and follow-up tasks. In `mvp` mode outputs a smoke report; in `full` mode adds coverage report, visual regression diffs, a11y matrix, and perf budget compliance. Use when qa-flow invokes it post-build or when user says "qa review", "test the build". Not for running tests ad-hoc (execution is external) or for replacing design-review / frontend-review (those handle visual critique; this handles test execution results).
---
```

**Step 3:** Verify:
- File exists.
- `grep -n "testingRigor" skills/qa-review/SKILL.md` → at least 3 matches.
- `grep -n "triage" skills/qa-review/SKILL.md` → at least 1 match.

**Step 4:** Commit: `qa-review: add post-build test execution review skill`

---

## Task 9: pm-architecture — add Testability subsection

**File:** `skills/pm-architecture/SKILL.md`

**Step 1:** Read the file.

**Step 2:** Find the sections emitted in the ARCHITECTURE.md template. After the existing technical sections (data model / API / etc.) and before any closing sections (open questions / decisions / etc.), add a new subsection.

The canonical add is a new `## Testability` section that references `testingRigor`:

```
## Testability

**testingRigor:** [shadcn-theme | custom-system — from PROJECT_PROFILE.md]

For `full` rigor, architecture MUST provide:
- Seams for mocking external dependencies (interfaces / ports / adapters)
- A dedicated test database or a migration-safe test schema
- API designed for contract testing (versioned, deterministic error shapes)
- Observability hooks accessible from tests (log capture, trace IDs)

For `mvp` rigor, these are nice-to-haves; defer until scope grows.

Flag any architectural decisions above that would block the chosen rigor level.
```

(Adjust the placeholder text to match the style of other sections in the ARCHITECTURE.md template already defined in pm-architecture.)

Also, in the `## How to run` section of the skill (not the template), add a step to read `.pm/<feature>/PROJECT_PROFILE.md` for `testingRigor` before generating the Testability subsection. Reuse the standard fallback prompt pattern if the file is missing.

**Step 3:** Verify:
- `grep -n "testingRigor" skills/pm-architecture/SKILL.md` → at least 2 matches.
- `grep -n "Testability" skills/pm-architecture/SKILL.md` → at least 2 matches.

**Step 4:** Commit: `pm-architecture: add Testability section referencing testingRigor`

---

## Task 10: pm-workstreams — add QA as 4th workstream

**File:** `skills/pm-workstreams/SKILL.md`

**Step 1:** Read the file.

**Step 2:** In Step 4 "Write the workstreams document" template, find the Workstream Overview table:
```
| Workstream | Owner(s) | Description | Depends on |
|-----------|----------|-------------|------------|
| Backend | [TBD] | [1-line scope] | Design (API contracts) |
| Frontend | [TBD] | [1-line scope] | Backend (APIs), Design (specs) |
| Design | [TBD] | [1-line scope] | PRD (requirements) |
```

Replace with:
```
| Workstream | Owner(s) | Description | Depends on |
|-----------|----------|-------------|------------|
| Backend | [TBD] | [1-line scope] | Design (API contracts) |
| Frontend | [TBD] | [1-line scope] | Backend (APIs), Design (specs) |
| Design | [TBD] | [1-line scope] | PRD (requirements) |
| QA | [TBD] | [1-line scope] | PRD (FR-XXX), Architecture (test seams), Design (a11y) |
```

**Step 3:** Find the subsequent detailed workstream sections (each starts with `## N. [Discipline] Workstream`). After the last one (likely `## 3. Frontend Workstream` or similar), add a new `## 4. QA Workstream` section with the same subsections as the others (Scope, What it owns, What it consumes, What it produces, Interface contracts, Definition of done). Scale content to match siblings — terse, one-liner per bullet.

Key content for the QA Workstream:
- Owns: test strategy, test cases, test automation code, test data/fixtures, QA review reports
- Consumes: PRD (FR-XXX, journeys), Architecture (test seams, testingRigor), Design (component specs for a11y), Backend (API contracts for integration tests), Frontend (routes + UI for E2E)
- Produces: QA_STRATEGY.md, TEST_CASES.md, QA_TASKS.md, QA_REVIEW.md, green/red status per release
- Interface contracts: with Backend (API contract tests block merge if strategy = full), with Frontend (E2E selector stability contract), with PM (green-for-release definition)

**Step 4:** Add a line to the workstream-specific questions in Step 2 (around "Does your team have dedicated backend, frontend, and design roles..."):
```
- "Is there a dedicated QA owner, or do devs own their own testing (full-stack pattern)?"
```

**Step 5:** Verify:
- `grep -n "QA Workstream" skills/pm-workstreams/SKILL.md` → at least 1 match.
- `grep -n "| QA |" skills/pm-workstreams/SKILL.md` → 1 match in the overview table.
- `grep -n "QA_STRATEGY" skills/pm-workstreams/SKILL.md` → at least 1 match.

**Step 6:** Commit: `pm-workstreams: add QA as 4th workstream`

---

## Task 11: pm-tasks — add QA section to TASKS.md template

**File:** `skills/pm-tasks/SKILL.md`

**Step 1:** Read the file.

**Step 2:** In the TASKS.md template, find the existing discipline sections (Backend / Frontend / Design). Add a parallel **QA** section at the end that mirrors their shape — summary line + reference to `.qa/<feature>/QA_TASKS.md` for detail + count of tasks + Effort breakdown.

**Step 3:** In the skill's `## How to run`, add a step to generate the QA section by reading `.qa/<feature>/QA_TASKS.md` if it exists. If it doesn't exist yet (qa-flow not run), emit a placeholder: `QA tasks: TBD — run \`qa-flow\` to generate.`

**Step 4:** Verify:
- `grep -n "QA_TASKS" skills/pm-tasks/SKILL.md` → at least 1 match.
- `grep -n "qa-flow" skills/pm-tasks/SKILL.md` → at least 1 match.

**Step 5:** Commit: `pm-tasks: add QA section cross-referencing QA_TASKS.md`

---

## Task 12: pm-handoff — generate QA_BRIEF + unified fallback prompt

**File:** `skills/pm-handoff/SKILL.md`

**Step 1:** Read the file.

**Step 2:** Update Step 1.5 "Read PROJECT_PROFILE.md". Replace the existing migration prompt:

Find:
```
> "Este projeto não tem PROJECT_PROFILE.md. Modo de design: **shadcn-theme** ou **custom-system**? (Escolha grava em `.pm/<feature-name>/PROJECT_PROFILE.md` — afeta design-tokens e design-components.)"
```

Replace with:
```
> "Este projeto não tem PROJECT_PROFILE.md completo.
> Modo de design: **shadcn-theme** ou **custom-system**?
> Rigor de teste: **mvp** ou **full**?
> (Respostas gravam em `.pm/<feature-name>/PROJECT_PROFILE.md` — afetam design-tokens, design-components, qa-strategy, qa-review.)"
```

Add a note after the prompt block:
```
If the file exists but is missing only one of the two fields (e.g., legacy project with `designMode` but no `testingRigor`), ask only for the missing field; preserve what's there.
```

**Step 3:** Update Step 2 "Ask which workstreams to hand off". Find the workstreams list:
```
- Design → gera `DESIGN_BRIEF.md` e invoca `design-flow`
- Backend → gera `BACKEND_BRIEF.md` e invoca `backend-flow`
- Frontend → gera `FRONTEND_BRIEF.md` e invoca `frontend-flow`
- All three
- "Just generate the briefings" → gera os três arquivos sem invocar nenhuma skill ainda
```

Replace with:
```
- Design → gera `DESIGN_BRIEF.md` e invoca `design-flow`
- Backend → gera `BACKEND_BRIEF.md` e invoca `backend-flow`
- Frontend → gera `FRONTEND_BRIEF.md` e invoca `frontend-flow`
- QA → gera `QA_BRIEF.md` e invoca `qa-flow`
- All four
- "Just generate the briefings" → gera os quatro arquivos sem invocar nenhuma skill ainda
```

**Step 4:** Add a QA_BRIEF.md template in Step 3. After the FRONTEND_BRIEF.md section (and its closing ``` fence), add a new section header `#### QA_BRIEF.md` with a template that includes:
- Header with designMode + testingRigor + uiFramework stamps (same pattern as DESIGN_BRIEF / FRONTEND_BRIEF)
- What we're testing (1 paragraph from PRD, QA perspective)
- testingRigor selected + implications
- FR-XXX list (what must be tested)
- User journeys to E2E
- External integrations to test (from ARCHITECTURE)
- Non-functional testing targets (perf, a11y, security — presence depends on testingRigor)
- Test environments and data strategy
- Tasks already defined (for reference) — from TASKS.md QA section

Save to `.qa/<feature-name>/QA_BRIEF.md`.

**Step 5:** Update Step 4 "Invoke specialist skill-sets". Add a QA block that parallels Design / Backend / Frontend:

```
**QA:**
> "QA_BRIEF.md is ready at `.qa/<feature-name>/`. Now invoking `qa-flow` — this runs the full QA workflow (qa-brief-intake → qa-strategy → qa-cases → qa-tasks → qa-review)."
>
> Run: `qa-flow`
> `qa-brief-intake` reads QA_BRIEF.md as its primary input and audits existing test infrastructure.
```

**Step 6:** Update Step 5 "Coordination note" to mention all four disciplines' parallelism:

Replace the parallel-work paragraph to include QA:
```
> "Design should start first — frontend needs the specs before building components. Backend can run in parallel with design. QA can start after PRD + architecture are done (in parallel with design/backend). Frontend waits on both design specs and backend contracts. Once everything is built, design-review, backend-review, frontend-review, and qa-review run in parallel."
```

**Step 7:** Verify:
- `grep -n "QA_BRIEF" skills/pm-handoff/SKILL.md` → at least 3 matches.
- `grep -n "testingRigor" skills/pm-handoff/SKILL.md` → at least 3 matches (migration prompt + QA_BRIEF template header + any body mentions).
- `grep -n "qa-flow" skills/pm-handoff/SKILL.md` → at least 2 matches.

**Step 8:** Commit: `pm-handoff: generate QA_BRIEF and unify profile migration prompt`

---

## Task 13: kanban-sync — ingest QA_TASKS.md

**File:** `skills/kanban-sync/SKILL.md`

**Step 1:** Read the file. Understand the current sources list (TASKS.md from PM + BACKEND_TASKS.md + FRONTEND_TASKS.md + DESIGN_TASKS.md).

**Step 2:** Add QA_TASKS.md as a 4th discipline source. Wherever the skill currently enumerates the three discipline TASKS files, add the QA file. Add the `qa` label to the labels map. Preserve the Epic → Task hierarchy and `Blocks` links.

Specifically, update:
- The sources list in the skill description / frontmatter if it mentions the 3 disciplines
- Any enumeration in the How to run steps
- The label mapping (add `qa: qa`)
- Any examples that show the 3 disciplines

**Step 3:** Verify:
- `grep -n "QA_TASKS" skills/kanban-sync/SKILL.md` → at least 2 matches.
- `grep -n "^.*qa:" skills/kanban-sync/SKILL.md` → at least 1 match in a labels block.

**Step 4:** Commit: `kanban-sync: ingest QA_TASKS.md as a 4th discipline source`

---

## Task 14: frontend-stack — read QA_STRATEGY for warn-conflict

**File:** `skills/frontend-stack/SKILL.md`

**Step 1:** Read the file.

**Step 2:** In Step 1 "Load intake", add a line after the PROJECT_PROFILE.md read:
```
Read `.qa/<feature>/QA_STRATEGY.md` if it exists. It declares testing tooling decisions that frontend-stack must be consistent with.
```

**Step 3:** After Step 2.5 "Profile conflict check" (the shadcn conflict check), add a Step 2.6:
```
### Step 2.6: QA tooling consistency check

If `.qa/<feature>/QA_STRATEGY.md` exists, cross-check the Testing row of the decision table against its tool choices:

- If frontend-stack picks a testing tool (e.g., Cypress) different from what QA_STRATEGY specified (e.g., Playwright) → **warn** in the decision log; do not abort:

  > "Nota: frontend-stack escolheu `[X]` para testes, mas QA_STRATEGY diz `[Y]`. Reconcile no decision log ou edite um dos dois. Não é bloqueante — ferramentas de teste são negociáveis."

Testing-tool conflicts are negotiable (dev and QA can reach alignment later); shadcn/ui is structural (Task 7 of the shadcn plan — aborts).
```

**Step 4:** Verify:
- `grep -n "QA_STRATEGY" skills/frontend-stack/SKILL.md` → at least 2 matches.
- `grep -n "Step 2.6" skills/frontend-stack/SKILL.md` → 1 match.

**Step 5:** Commit: `frontend-stack: warn on QA_STRATEGY tooling conflicts`

---

## Task 15: backend-stack — read QA_STRATEGY for warn-conflict

**File:** `skills/backend-stack/SKILL.md`

**Step 1:** Read the file.

**Step 2:** Mirror Task 14 for backend-stack: add a QA_STRATEGY read in Step 1, add a conflict-check step that warns (does not abort) when backend testing tool choice conflicts with qa-strategy. Typical conflicts: backend picks pytest but QA says Jest (no, different languages wouldn't conflict) — more realistic: backend picks supertest vs qa says using the API via Playwright, or coverage tool mismatch (nyc vs c8 vs v8's native).

**Step 3:** Verify:
- `grep -n "QA_STRATEGY" skills/backend-stack/SKILL.md` → at least 2 matches.
- `grep -n "warn" skills/backend-stack/SKILL.md` → at least 1 match in the conflict section.

**Step 4:** Commit: `backend-stack: warn on QA_STRATEGY tooling conflicts`

---

## Task 16: Update README and docs/skills-flow.md

**Files:** `README.md`, `docs/skills-flow.md`

**Step 1:** Read both files.

**Step 2:** `README.md` edits:

- Top-of-file line "35 skills" → "41 skills" (adjust to reflect added skills: 35 + 6 qa-* = 41).
- Flow diagram (the ASCII tree near the top): add a new `qa-flow` column alongside `design-flow` / `backend-flow` / `frontend-flow`. Each row: qa-brief-intake → qa-strategy → qa-cases → qa-tasks → (build) → qa-review.
- "Design modes" section: rename to "Project profile" or add a sibling "Testing rigor" section. Document `testingRigor: mvp | full` with the same compactness as the design modes section. Mention the 3 additional readers (pm-architecture, qa-strategy, qa-review).
- Artifacts on disk section: add a `.qa/<feature>/` block listing the 6 qa artifacts.
- `.pm/<feature>/` block: no change needed (PROJECT_PROFILE already listed; schema extension is implicit).

**Step 3:** `docs/skills-flow.md` edits:

- Add a new mermaid diagram "QA flow (com testingRigor branching)" mirroring the design-flow diagram's shape
- Update the top-level orchestration diagram to include `qa-flow` as a 4th parallel branch from pm-handoff, with `qa-review` in the post-build fan-out
- Update the PROJECT_PROFILE.md read/write map to add `qa-strategy`, `qa-review`, `pm-architecture` as readers
- Update the artifacts table with the `.qa/<feature>/` row

**Step 4:** Verify:
- `grep -n "qa-flow" README.md` → at least 3 matches.
- `grep -n "testingRigor" README.md` → at least 2 matches.
- `grep -n "qa-flow" docs/skills-flow.md` → at least 3 matches.
- `grep -n "testingRigor" docs/skills-flow.md` → at least 2 matches.
- `grep -n ".qa/<feature>" README.md` → at least 1 match.

**Step 5:** Commit: `docs: document qa-flow and testingRigor in README and flow map`

---

## Task 17: End-to-end read-through verification

**Files:** (read-only verification)

**Step 1:** Walk the full flow mentally using the edited and new skills. Order:

1. `pm-grill` — Testing profile branch, PROJECT_PROFILE.md schema extended.
2. `pm-intake` — test infra detection row.
3. `pm-architecture` — Testability subsection references testingRigor.
4. `pm-workstreams` — QA as 4th workstream.
5. `pm-tasks` — QA section in TASKS.md cross-referencing QA_TASKS.md.
6. `pm-handoff` — unified migration prompt, QA_BRIEF generated, qa-flow invoked.
7. `qa-flow` → `qa-brief-intake` → `qa-strategy` (branches on testingRigor) → `qa-cases` (branches) → `qa-tasks` → `qa-review` (branches).
8. `kanban-sync` — ingests QA_TASKS.md.
9. `frontend-stack` / `backend-stack` — warn on QA_STRATEGY tooling conflicts.
10. `README.md` + `docs/skills-flow.md` — QA represented.

**Step 2:** Cross-file consistency greps:

```bash
# All skills that should mention testingRigor or read PROJECT_PROFILE
Grep "testingRigor" skills/pm-grill/SKILL.md skills/pm-intake/SKILL.md skills/pm-architecture/SKILL.md skills/pm-handoff/SKILL.md skills/qa-flow/SKILL.md skills/qa-strategy/SKILL.md skills/qa-review/SKILL.md skills/qa-cases/SKILL.md -l
```

Expected: all 8 files match.

```bash
# All skills that should reference QA_BRIEF
Grep "QA_BRIEF" skills/pm-handoff/SKILL.md skills/qa-flow/SKILL.md skills/qa-brief-intake/SKILL.md -l
```

Expected: all 3 match.

```bash
# kanban-sync + all *-tasks should be wired
Grep "QA_TASKS" skills/kanban-sync/SKILL.md skills/pm-tasks/SKILL.md skills/qa-tasks/SKILL.md -l
```

Expected: all 3 match.

**Step 3:** Spot-check migration prompt consistency. Open `pm-handoff`, `design-flow`, and `qa-flow`. All three should either:
- Use the same unified prompt (preferred), OR
- Clearly reference the canonical prompt

If `design-flow` still has the shorter version from the shadcn-mvp-mode work, update it to the unified form (it was left as is there; this task brings it in line).

If drift is found, open a follow-up task within this plan to fix it.

**Step 4:** No commit (read-only verification).

---

## Out of scope for this plan

- `testingRigor: standard` (middle value) — binary is enough for v1.
- Security-specific QA flow — out.
- Continuous performance QA — part of `qa-review full`, not a separate flow.
- Shared Gherkin library across features — not in v1.
- Integration with `superpowers:test-driven-development` — complementary discipline, no wiring needed.

---

## Execution Handoff

Plan complete and saved to `docs/plans/2026-04-19-qa-flow.md`. Given the size (17 tasks, most edits mechanical but 6 new skill files require content judgment), **Subagent-Driven execution is the right approach**: fresh subagent per task, spec + quality reviews between, immediate fix loops.

Continuing into subagent-driven-development.
